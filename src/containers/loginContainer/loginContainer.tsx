import { FunctionComponent} from 'react';

interface Props {
  
}

const Login: FunctionComponent<Props> = ({})  => {
    return (
        <div>
            Login
        </div>
    );
}

export default Login;
